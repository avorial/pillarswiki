# Pillars of Salt CampaignRepo Wiki

This repository now includes a CampaignRepo-compatible wiki in `/wiki`.

- Pages: `/wiki/pages`
- Media: `/wiki/media`
- Config: `/wiki/campaign.yaml`

The import is generated from `chronicle-data.json`, `cleaned-characters.js`, and `pictures/`.
